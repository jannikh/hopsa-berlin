    {# {"name": "Kontakt", "path": "/kontakt"}, #}
{% set navigation = [
    {"name": "Home", "path": "/"},
    {"name": "Galerie", "path": "/galerie"},
    {"name": "Social Media", "path": "/links"},
    {"name": "Kurse", "path": "/kurs"},
    {"name": "Aufführungen", "path": "/termine"}
] %}

<header class="bg-black text-white">
    <nav class="container mx-auto py-0 px-0 sm:px-6">
        <div class="max-w-4xl mx-auto">
            <div class="x-bg-red-900 min-w-full flex items-center justify-between overflow-x-auto px-6 sm:px-0 -ml-6">
                    <div class="flex items-center">
                    {% for link in navigation %}
                            {% set isActive = page.url == link.path or (page.url | slice(0, link.path | length)) == link.path %}
                            <a class="text-white hover:text-gray-300 py-3 my-3 px-6 border-b-2 border-black hover:border-red-800 hover:text-red-800 transition-all duration-200 {% if isActive %}border-white{% endif %}" href="{{ link.path }}">{{ link.name }}</a>
                    {% endfor %}
                    </div
                    <div>
                    <a href="/kurs" class="ml-6 px-4 py-2 border border-white rounded-sm hover:bg-white hover:text-black transition-colors whitespace-nowrap">Sei Dabei</a>
                    </div>
            </div>
        </div>
    </nav>
</header>